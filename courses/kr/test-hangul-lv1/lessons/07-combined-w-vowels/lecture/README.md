The deck goes here as `index.html`.

Both slots are mandatory before this lesson can ship — a lesson with only a
수업용 deck leaves PRESTUDY_LEMONBOARD_KEY empty and class creation then fails
at /rooms/null/duplicate. `tools/plan.py` marks it blocked until then.

Delete this file once the deck exists.
